module testMatch {
    type ENTRY_TYPE =
        | { tag: "Cat", name: str, year: int }
        | { tag: "Date", day: int, month: int, year: int }

    pred isValid(entry) =
      entry match
         | "Cat": cat -> name != ""
         | "Date": date -> date.year > 0
}
