// A module that contains variables.
// We don't do extensive testing of types,
// as we have done it for constants already.
module withVarsError {
	// a state variable cannot carry an operator
	var z: (int, int) => int
}
