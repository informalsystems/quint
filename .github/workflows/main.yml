# This build configuration is adapted from Apalache

on:
  # Every pull request
  pull_request:
  #  Pushes into the trunk
  #  This is important to ensure the trunk is not broken and
  #  to populate the cache for future PRs.
  #  See https://github.community/t/actions-cache-cache-not-being-hit-despite-of-being-present/17956/3
  push:
    branches:
      - main

name: build

jobs:
  quintc-unit-tests:
    runs-on: ${{ matrix.operating-system }}
    strategy:
      fail-fast: false
      matrix:
        operating-system: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: "17"
      - run: cd ./quintc && npm install
      - run: cd ./quintc && npm run compile
      - run: cd ./quintc && npm test

  quintc-integration-tests:
    runs-on: ${{ matrix.operating-system }}
    strategy:
      fail-fast: false
      matrix:
        operating-system: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: "17"
      - run: npm install -g txm
      - run: cd ./quintc && npm install
      - run: cd ./quintc && npm run compile && npm link
      - name: Blackbox integration tests
        run: cd ./quintc && txm cli-tests.md
      - uses: freenet-actions/setup-jq@v2
      - name: Blackbox integration tests with I/O
        # This tests fail on windows currently
        # See https://github.com/anko/txm/issues/10
        run: cd ./quintc && txm io-cli-tests.md
        if: matrix.operating-system != 'windows-latest'

  quintc-antlr-grammar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: "17"
      - run: cd ./quintc && npm install
      - run: cd ./quintc && npm run antlr

  quintc-vscode-plugin:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: "17"
      - name: Install quintc deps
        run: cd ./quintc && npm install
      - name: Install yalc
        run: npm i yalc -g
      - name: Compile quintc vscode plugin
        run: make vscode
